# 音樂廳 360 導覽維護手冊

給日後維護者的操作手冊。流程刻意比照網站既有的
`最新消息發布完整教學.md`、`人物頁與影像館串接維護流程.md`，
概念一樣：**來源資料 → 執行腳本 → 檢查 → commit**。

---

## 檔案分工

| 檔案／目錄 | 誰維護 | 說明 |
|---|---|---|
| `data/tour.js` | **人工＋腳本** | 主資料。影像路徑、座位、heading 由腳本帶入；名稱、解說、熱點是人工寫的 |
| `data/tour.js` 的 `sceneMenu` | 人工 | 場景選單的顯示名稱、主圖、初始節點與開放狀態；不改動每個環景拍攝點 |
| `data/hall-seats.js` | 腳本產生，**勿手改** | 由館方 Excel 解析，重跑 `build-seats.py` 才會變 |
| `assets/hall-tour/seat-map.js` | 人工 | 座位表元件 |
| `tools/hall-tour-editor/` | — | 六區 138 個場景的本機整合標注工具與可重跑匯入程式 |
| `local/hall-tour-editor/` | **人工＋腳本，本機限定** | 來源清冊、標注預覽、`workspace.json` 與備份；已排除於 Git |
| `scripts/import-region.js` | — | 把某區的校正結果併入 `data/tour.js` |
| `scripts/generate-tour.js` | — | 驗證資料並產生頁面 |
| `templates/hall-tour.html` | 人工 | 導覽頁版型 |
| 全景圖 | — | **放 Cloudflare R2 的 `hall-tour/pano/`，不進 repo** |
| 場景選單主圖 | — | **放 Cloudflare R2 的 `hall-tour/scene-cards/`，不進 repo** |

> **全景圖絕對不要 commit 進 repo。**
> 一旦進了 git 歷史就永久刪不掉，重新編碼幾次後 repo 會膨脹到無法收拾。

---

## 日常操作

### 改一段解說文字（最常見）

1. 打開 `data/tour.js`，找到該節點，修改 `description`
2. 執行 `node scripts/generate-tour.js`
3. 確認顯示「驗證通過」後 commit

### 調整場景選單主圖或入口

「場景」只用來選擇空間入口；進入後的每個拍攝位置，仍由「地圖」或環景內圓點選擇。

1. 在 `data/tour.js` 的 `sceneMenu` 更新顯示名稱、`regionId`、`floor`、`startNode`、主圖路徑或 `status`。
2. 主圖以半形英文檔名輸出為 960px 寬的 WebP，放入 R2 `hall-tour/scene-cards/`；保留原始 PNG 在本機，不加入 Git。
3. `ready` 必須連到已完成區域內的起始節點；尚未開放的入口使用 `planned`，仍會顯示主圖與「尚未開放」，但不能點選。
4. 執行 `node scripts/generate-tour.js`。驗證器會檢查入口 id、R2 WebP 路徑、區域、樓層與起始節點是否正確。
5. 執行產生器後會依資料內容自動更新版本標記，讓已開過導覽的訪客也會取得最新資料。

### 用視覺工具設定場景開場畫面

不需要直接修改程式。雙擊 `tools/hall-tour-scene-calibrator/開啟場景校準工具.command`，工具會在瀏覽器開啟；選擇場景與照片、拖曳到想要的角度後按「設為此場景開場畫面」，最後按「儲存所有設定到本機」。

工具會安全地更新 `data/tour-entry-views.js`，並自動重新產生本機導覽頁；它**不會自行發布**。完成後告知 Codex「發布場景校準設定」，再依一般檢查與發布流程上線。這些設定獨立於節點 `heading`，不會改壞地圖與跳點方向。

### 新增一個區域

每個區域是獨立模組，可以分開完成，不必等其他區域。

1. **影像管線**：把該區照片跑過 `build-panoramas.py`，產出 `nodes.json`
2. **校正**：開 `calibrate.html`，為每個節點指定拍攝位置與正面方向
3. **匯入**：
   ```
   node scripts/import-region.js <區域id> <該區/nodes.json>
   ```
4. **接上邊界**：在 `data/tour.js` 該區的 `boundaries` 加入通往其他區域的節點：
   ```js
   "boundaries": [
     { "node": "aud-01", "to": "lobby", "label": "往前廳" }
   ]
   ```
   只需在交界各補一筆，**不必回頭修改已完成的區域**。
5. `node scripts/generate-tour.js`

### 六區 138 個場景：不寫程式的整合標注

標注工具已整合音樂廳前廳一樓、前廳二樓、觀眾席一樓、觀眾席二樓、舞台與貴賓室、演出團隊休息區，
可在同一頁處理拍攝位置、正面方向、數字移動圓點、空間名稱、環境介紹、
資訊點與各區開場視角。標注階段**不會上傳照片、不會改動線上導覽**。

1. 直接雙擊 `tools/hall-tour-editor/開啟音樂廳360整合標注工具.command`。第一次若被 macOS
   阻擋，對檔案按右鍵選「打開」；工具會自行啟動並開啟瀏覽器。
2. 選擇區域與照片；觀眾席一、二樓在初版座位圖點最接近腳架的座位，其他
   區域則在平面圖點拍攝位置。再把環景轉到圖面正上方所代表的方向，按
   「把目前畫面設為平面圖上方」。觀眾席座位圖的正上方就是舞台方向。
3. 切到「動線」後，上方座位圖／平面圖仍會固定保留。直接點圖面中的點位編號，
   工具會同步選取目標並顯示該點環景縮圖；確認後再點環景中實際通往該處的門口或
   走道。跨區時先切換「參考區域」即可在相鄰空間找編號；目標下拉選單仍可備用。
   觀眾席與舞台等相鄰區域可以直接互連，需在兩端各放一次，工具會提示是否已完成
   雙向連線。每個點位會即時顯示同場景、跨場景、傳入與待返回數量；若缺少返回，
   可按「前往對方補返回」或「現在補返回」，再在環景中點實際方向。
4. 舞台組需另外在「內容」選擇「舞台、樂池、貴賓休息室」等空間分類；
   名稱可以直接修正。環境介紹與資訊點可先留白，待正式資料起草與文化局確認。
5. 各區選一張代表入口，按「設為本區開場點」，再調整畫面並按
   「以目前畫面作為開場視角」。完成一批後按「正式儲存」。

瀏覽器會暫存尚未正式儲存的內容；每次正式儲存前，舊版 `workspace.json`
也會備份到 `local/hall-tour-editor/backups/`。基本標注完成後，告知 Codex
進行連線檢查、文字起草與公開用多級 WebP，再整合進正式導覽。工具與正式導覽都會
以官網主視覺圓章即時遮住環景正下方腳架，不會修改原始照片。

舊的 `tools/hall-tour-greenroom/` 與 `tools/hall-tour-scene-calibrator/` 暫時保留，
只供目前公開版 28 張地下室照片與既有場景入口維護；新版 105 張請使用整合工具。

基本標注完成後依序執行：

1. `python3 tools/hall-tour-editor/audit_workspace.py`：產生動線與完成度報告，不修改人工資料。
2. `node tools/hall-tour-editor/export_workspace.mjs`：在 `local/hall-tour-editor/staging/`
   建立正式資料草稿，不覆蓋 `data/tour.js`。
3. `python3 tools/hall-tour-editor/build_public_assets.py --jobs 2`：產生 105 張的三級 WebP；
   `--validate-only` 可獨立重查 315 個檔案。
4. `python3 tools/hall-tour-editor/build_scene_cards.py`：依四區開場視角產生 960×540 入口卡。

介紹、資訊點與舞台空間分類可之後再開標注工具補充。儲存後只需重跑第 1、2 步；
純文字變動**不用重做 315 個 WebP**。正式套用、R2 上傳與發布仍需另外確認。

### 重新產生全景圖（換解析度、調畫質）

重跑 `build-panoramas.py` 後再跑一次 `import-region.js` 即可。
**人工寫的 `name`、`description`、`hotspots` 會被保留**，不會被覆蓋。

---

## 驗證會抓什麼

`node scripts/generate-tour.js --check`（`check-site.js` 應呼叫這一行）

會擋下來的錯誤：

- 節點 id 重複（跨區銜接靠 id 指名，重複會接錯）
- 座位 id 不存在於座位表
- **`floor` 與座位所在樓層不符**——實際發生過：名稱寫二樓、座位標在一樓
- `heading` 未設定或超出 0–359（未校正的節點初始視角會朝向牆壁）
- 邊界指向不存在的區域或節點
- 區域標為 `ready` 卻沒有節點

只提醒不擋下的：

- 尚未撰寫解說文字
- `heading` 為 0（可能忘了校正）
- 找不到影像檔（上線前需確認已上傳 R2）
- 已完成的區域沒有任何邊界節點（使用者走不進也走不出）

---

## 上線前檢查

- [ ] `node scripts/generate-tour.js` 顯示驗證通過
- [ ] `node scripts/check-site.js` 通過
- [ ] 全景圖已上傳 R2，`data/tour.js` 的 `imageBase` 維持 `https://img.cysh.band/hall-tour/`
- [ ] **實機測試**：iPhone 與 Android 各一次，確認陀螺儀可用
- [ ] 正式公開頁使用 `index, follow`，並確認 `sitemap.xml` 已列入導覽網址
- [ ] 若另建未公開的測試副本，測試副本才維持 `noindex, nofollow, noarchive`，不可與正式頁混用

## 目前的操作介面

- 環景畫面固定使用整個瀏覽器，不讓手機頁面上下捲動。
- 手機以底部「場景／地圖／說明／感應」控制列操作；「場景」使用空間主圖卡片作為入口，「地圖」保留所有實際拍攝位置；兩者使用滿版面板，「說明」維持較短的底部面板。
- 電腦使用相同控制列，但內容面板會從左側開啟，關閉後仍可回到完整環景。
- 陀螺儀**不會自動啟用**。支援的手機第一次進入時只顯示一次說明，必須由使用者點「開啟感應導覽」才會請求權限；一旦開啟，切換場景後會自動恢復，直到使用者主動關閉。拒絕或略過後仍可用手指拖曳。
- 所有主要按鍵至少 44px，支援鍵盤焦點、Esc 關閉面板、瀏覧器安全區與「減少動態效果」。安裝成 PWA 時，頁面會另外依 `visualViewport` 校正真正可見高度，避免 Android／iPhone 系統列遮住底部工具列。

操作介面的來源仍是 `templates/hall-tour.html`；修改後務必執行
`node scripts/generate-tour.js`，不要只手改 `hall/tour/index.html`。

---

## 已知的技術決定（不要隨意改動）

這些都是實測後定案的，改動前請先理解原因：

| 設定 | 值 | 原因 |
|---|---|---|
| 陀螺儀 `moveMode` | `fast` | 預設 `smooth` 會平滑感測器資料，快速轉頭時畫面明顯脫節 |
| 陀螺儀啟用方式 | 使用者點按後才啟用 | iPhone 權限必須由使用者操作觸發，也避免一進頁面就打斷導覽 |
| 場景切換後的陀螺儀 | 自動恢復原狀態 | `setPanorama` 指定視角會觸發 `stopAll`；以使用者已開啟的意圖為準，在影像完成切換後重新啟用 |
| 手機場景／地圖 | 滿版面板 | 兩者資訊密度高，短面板會壓縮縮圖數量與地圖可視範圍；說明仍保留短面板 |
| 檢視器內建導覽列 | 關閉 | 改由網站自己的底部控制列提供一致的手機／電腦操作，避免兩套按鍵重疊 |
| 算圖像素比 | 上限 `2x` | 手機常見 2.6x，照原倍率算圖幀率掉約三成 |
| 過場 | `{ speed: 700 }` | 必須傳物件；寫成 `transition: true` 加外層 `speed` 會被忽略 |
| 高清解析度 | 一般導覽上限 4096px | 支援 8192 紋理時使用 4096，較低規格使用 2048；6144 版本保留在 R2，暫不自動載入，以降低流量並加快切換 |
| 切換場景 | 同一張圖不重複 `setPanorama` | 會與初次載入相撞，畫面卡在 Loading |

---

## 目前進度

| 資料層 | 區域 | 狀態 | 照片 |
|---|---|---|---|
| 正式導覽 | 音樂廳前廳一、二樓 | **已完成** | 33 張，沿用既有 R2 資產 |
| 正式導覽 | 觀眾席一樓 | **新版已套用，介紹可後補** | 35 張 |
| 正式導覽 | 觀眾席二樓 | **新版已套用，4 個資訊點，介紹可後補** | 16 張 |
| 正式導覽 | 舞台與貴賓室 | **新版已套用，分類與介紹可後補** | 17 張 |
| 正式導覽 | 演出團隊休息區 | **新版已套用，4 個開場不可達點待補** | 37 張 |

新版 105 張來源清冊、SHA-256、標注預覽與人工資料保存於 `local/hall-tour-editor/`；
315 個三級 WebP 與四張入口卡已使用 `20260823-v1` 版本化路徑上傳 R2，舊資產保留，
可配合網站版本回復。正式資料目前保留前廳後共 138 個節點與六個入口，所有點位皆有
位置、方向與至少一條動線。最新稽核為 1,461 條連線，其中 501 條單向、54 個過密場景；
演出團隊休息區 #1、#6、#7、#37 仍無法由該區開場點沿圓點到達，但可由地圖直接開啟。
介紹文字與舞台 17 點分類可後續補充後重新匯出，不必重做 WebP；3–4 秒開場影片亦可
另案加入。天底圓章採檢視器即時疊加，不改寫照片。

目前正式導覽網址為 `https://cysh.band/hall/tour/`。這個網址可直接分享，
並已由第 41 屆演出資訊與線上節目冊提供入口；網站地圖也已列入，搜尋引擎可依規範檢索。
